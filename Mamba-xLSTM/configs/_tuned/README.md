Generated by ``scripts/run_optuna_search.py`` as ``{dataset}_{model}.yaml``.
These files overlay ``train.*`` / ``model.*`` keys onto the base YAMLs via
``scripts/train.py --config-overlay``.
Remove or regenerate when you change preprocessing or splits.
